files:
  "/etc/php.d/wordpress-config.ini":
    mode: "000755"
    owner: root
    group: root
    content: |
      ; Increase the maximum upload size
      upload_max_filesize=999M
      post_max_size=999M